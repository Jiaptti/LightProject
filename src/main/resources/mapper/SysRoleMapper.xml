<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.viroyal.light.module.dao.user.SysRoleMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.viroyal.light.module.entity.user.SysRole">
		<result column="id" property="id" />
		<result column="name" property="name" />
		<result column="type" property="type" />
	</resultMap>

	<select id="getRoleListById" parameterType="string" resultType="com.viroyal.light.module.entity.user.SysRole">
		select r.type from sys_role as r where r.id in (select ur.uid from sys_user_role as ur , sys_user as u where u.id = #{uid})
	</select>
</mapper>
